{
  "name": "fedjobs-monorepo",
  "private": true,
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "test": "turbo run test",
    "web:dev": "pnpm --filter @fedjobs/web dev",
    "api:deploy": ""

  },
  "devDependencies": {
    "typescript": "^5.0.0",
    "turbo": "^1.10.0",
    "@types/node": "^20.0.0",
    "prettier": "^3.0.0",
    "eslint": "^8.0.0"
  }
}